{
  "name": "sylvanyu-io",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:video-manifest": "node scripts/generate-video-byte-lengths.mjs",
    "check:video-manifest": "node scripts/generate-video-byte-lengths.mjs --check",
    "predev": "npm run sync:video-manifest",
    "dev": "astro dev",
    "prebuild": "npm run sync:video-manifest",
    "build": "rm -rf dist && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@galacean/engine": "1.1.0-beta.14",
    "@galacean/engine-toolkit": "1.1.0-beta.0",
    "astro": "^6.4.2",
    "three": "^0.184.0",
    "vite-plugin-glsl": "^1.6.0"
  }
}
